# Borrowing a Game (with a Barcode Scanner)

Use this guide when your station has a barcode scanner. Scanning is the fastest way to borrow a game, but the steps are mostly the same as the manual process.

---

## What You Need

- A barcode scanner connected to your station.
- A game with a barcode label on the box.
- The patron's badge (it has a barcode on it).

---

## Steps

### 1. Go to the Borrow page

<!-- TODO: screenshot — Borrow_Page.png (barcode field visible) -->
![The Borrow page with the Barcode scan text field visible in the toolbar](img/Borrow_Page.png)

Select the **Borrow** tab in the navigation bar at the **top right of the screen** — or press **Tab** on your keyboard until **Borrow** is selected, then press **Enter**.

You will see a **Barcode** scan text field in the toolbar near the top of the page. This is where scanned barcodes will go.

---

### 2. Scan the game's barcode

Point the scanner at the barcode label on the game box and scan it. The app will look up the game right away and the borrow pop-up window will open automatically.

**An error banner (shown in red) appears at the bottom of the screen with the message "No available games with this barcode":**
This game's barcode is not in the app yet. Do the following:
1. Use the [manual borrow process](borrow-manual.md) to search for and borrow the game by name instead.
2. Let someone know that this game's barcode needs to be added to the system.

---

### 3. The borrow window opens

<!-- TODO: screenshot — Borrow_Modal.png -->
![The borrow pop-up window showing the game title and a patron name field](img/Borrow_Modal.png)

The borrow window will show the name of the game. Now you need to identify the patron.

---

### 4. Scan the patron's badge

Point the scanner at the barcode on the patron's badge and scan it.

- **If the patron is already in the system**, their name will be filled in automatically. Skip to Step 6.
- **If an Add Patron window appears**, the patron is not in the system yet. Go to Step 5.

---

### 5. Add a new patron (if needed)

If the patron is not in the system, an **Add Patron** window will appear. Their scanned badge barcode will already be filled in.

Ask the patron for their **full name** and type it into the name field, then save. The borrow window will come back with their name filled in.

> **Note:** For full instructions on adding a patron, see [Adding a Patron](add-patron.md).

---

### 6. Finish the borrow

Check that the game title and patron name are correct, then click **Borrow** to complete the checkout — or press **Tab** to move to the button, then press **Enter** or **Space**.

A success banner (shown in green) will appear at the **bottom of the screen** confirming the checkout was completed. It will close on its own after a moment.

The game will no longer show as available in the Borrow list. The checkout is complete.

---

## Troubleshooting

**The scanner does not seem to be working:**
Click directly on the **Barcode** scan text field on the page (or inside the borrow window), then try scanning again. Clicking the field first makes sure the scanner knows where to send the barcode.

---

## Notes

- A game can only be borrowed by one person at a time. Scanning a game that is already checked out will show an error.
- If an error banner (shown in red) appears at the bottom of the screen and the reason is not clear, ask for help or write down what you were doing so someone can look into it later.

---

*See also: [Borrowing Without a Scanner](borrow-manual.md) · [Returning with a Barcode Scanner](return-barcode.md) · [Adding a Patron](add-patron.md)*
